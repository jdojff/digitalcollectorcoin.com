export default class TokenMeta {
    constructor(tokenId, serialNumber, denomination, cityName, image) {
        this.tokenId = tokenId;
        this.serialNumber = serialNumber;
        this.denomination = denomination;
        this.cityName = cityName;
        this.image = image;
    }
}